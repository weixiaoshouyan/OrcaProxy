# 2026-06-12

## Bug 检查与修复

审查了用户自修改后的代码变更（src/index.ts +602行, Chat.tsx +701行），发现并修复以下问题：

### P0 - 崩溃级
- **URL 双 /v1 拼接**: `compressContextIfNeeded` 中 `baseUrl + "/v1/chat/completions"` 当 baseUrl 已含 /v1 时产生 404。修复为 normalize trailing slashes 后判断。
- **resolved 空值崩溃**: `compressContextIfNeeded` 未检查 `resolved?.provider?.baseUrl`，添加了 early return guard。

### P1 - 功能级
- **fetch 无超时**: 压缩请求的 fetch 无 AbortSignal，设置 20s 超时 + AbortError 专门处理。
- **messages 大小估算 bug**: content 为数组时 .length 返回元素数而非字符串长度，改为类型判断后 JSON.stringify。

### P2 - 体验/安全
- **React.memo 失效**: `trackFileOperation` 每次渲染新引用，改为 `useCallback` 包裹。
- **Shell 注入**: git commit -m 拼接用户输入，改用 temp file + `git commit -F`。

### P3 - 清理
- 移除未使用的 rimraf devDependency。

### 长任务稳定性确认
- ✅ req.socket.setTimeout(0) 正确设置
- ✅ server.timeout/keepAliveTimeout/requestTimeout = 0
- ✅ tool 执行期间 15s keep-alive ping
- ✅ 递归深度限制 maxDepth=40
- ✅ 消息数组 800KB 截断
- ✅ 工具输出 30KB 截断
- ✅ 上下文 15000 token 自动压缩

## 构建验证
- TypeScript 编译: 零错误通过
- 前端引用完整性: 已确认
